# ML Layer (backend-only)

This folder is reserved for ML/analytics implementations used by the Nethra app.

Constraints (for this hackathon build):
- UI/routes/components must remain unchanged.
- Only backend logic may be modified.

Future work:
- Replace synthetic scoring with ASTraM-derived estimators.
- Provide interfaces consumed by existing Nethra lib functions.

